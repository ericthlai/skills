# Force LF in every working tree, on every platform.
#
# Git for Windows defaults to core.autocrlf=true, so without this a Windows clone
# checks these files out with CRLF and the frontmatter opens as "---\r\n". Whether
# a given skill loader tolerates that is not something this repo should depend on.
* text=auto eol=lf
